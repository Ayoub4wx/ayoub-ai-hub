[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Map /_next/static/* → /static/* on CDN (publish root is .next)
[[redirects]]
  from = "/_next/static/*"
  to = "/static/:splat"
  status = 200
  force = true
